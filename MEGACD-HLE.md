# メガCDのBIOSなし起動

`src/Gens/bin/Release/gens.exe` を起動し、File → Open ROM で
`src/SampleGame/FHB.CUE` を開いてください。BIOSが設定されていない場合は、
内蔵のBIOS呼び出しエミュレーション（HLE）で起動します。
別のBIOSファイルのダウンロードや環境変数の設定は不要です。
設定済みのBIOSを読み込める場合は、従来の起動方法が優先されます。

`FHB.BIN` を開いた場合も、同じフォルダーの `FHB.CUE` を読み込みます。
CD音声のトラック情報を使うため、この2ファイルは一緒に置いてください。
パッドのStartでデモからタイトルへ進み、STARTを選択します。
名前入力後はStartで確定できます。キー割り当てはエミュレーターの
コントローラー設定に従います。

## 保存

ゲーム内の「セーブ」を使用してください。HLEの保存データはBRAM保存先
（未設定なら実行ファイルのフォルダー）の `segacd-XXXXXXXX.hbr` に保存します。
ゲームを開き直してCONTINUEを選ぶと再開できます。
通常BIOS用の `.brm` とは別形式で、相互変換には対応していません。
HLE使用中のステートセーブ／ロードは無効です。

## 対応範囲

実装は部分的なBIOS APIの代替です。BIOSのバイナリや起動画面は含みません。
ゲームが要求するCD読み込み、割り込み、VDP・パッド処理、CD音声、
ゲーム内セーブを処理します。CDプレイヤー画面はありません。

動作確認対象は提供された『惑星ウッドストック ～ファンキーホラーバンド～』です。
タイトル、名前入力、本編開始、移動、コマンド、セーブと再起動後のCONTINUEを
確認しています。全編のクリアや他タイトルの互換性を保証するものではありません。
未対応のBIOS呼び出しに到達すると、呼び出し番号を画面に表示して一時停止します。

CUEの対応形式は、1つのBINARYファイル内のMODE1/2352データトラックと
AUDIOトラックです。複数ファイルのCUE、圧縮ディスク形式には対応していません。
CPUデバッガーの命令単位実行はHLEの検証対象外です。

## 開発者向け検証

Visual Studio 2022のC++ツールとPythonが必要です。

```powershell
python tests/run_cd_hle_tests.py --image src/SampleGame/FHB.CUE
```

Releaseをビルドし、CUEの境界・不正入力テストと、通常／精密タイミングの
ゲーム開始・保存・CONTINUEを各9000フレーム検証します。
CD音声のミキサー出力、保存APIのエラー処理、リセット、終了も検査します。
結果のBMP画像・ログ・テスト用セーブは `src/Gens/bin/cd-hle-tests/run-*` に作成し、
普段のセーブには触れません。ゲームデータはテストに同梱していません。
`--image` を省略するとCUEパーサーのみ、`--build-only` ならビルドのみ実行します。

診断が必要な場合だけ、`GENS_HLE_TRACE` にログファイルのパスを指定してください。
`GENS_HLE_SAVE_DIR` はテスト用の保存先変更に使用できます。
